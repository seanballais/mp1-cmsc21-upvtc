/*
 * DisplayView.c
 *
 * Contains the functions that handles any text display related
 * tasks.
 *
 */

#include "DisplayView.h"

void
DisplayView_printMenu(const unsigned int userRole)
{
    /*
     * Prints a menu that displays the tasks that are available
     * to a user privilege.
     */

    if (userRole == ADMIN) {

    }
}
